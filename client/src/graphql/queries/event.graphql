query Event($id: String!) {
  event(id: $id) {
    ...RegularEvent
    attendees {
      id
      firstname
      lastname
      email
      phoneNumber
      beemId
      createdAt
      updatedAt
    }
  }
}
