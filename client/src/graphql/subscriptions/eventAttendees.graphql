subscription eventAttendees($id: Float!) {
  eventAttendees(id: $id) {
    eventId
    attendeeId
    isConfirmed
    attendee {
      id
      lastname
      firstname
      email
      beemId
      phoneNumber
      createdAt
      updatedAt
    }
  }
}
