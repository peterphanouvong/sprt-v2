import { shallow } from "enzyme";
import { InputField } from "./InputField";

test("Input field snapshot", () => {
  // expect(
  //   shallow(<InputField name="username" label="Username" />)
  // ).toMatchSnapshot();
});
